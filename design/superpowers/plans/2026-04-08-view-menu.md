# 视图菜单实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在编辑器工具栏添加视图下拉菜单，控制伤害事件卡片显示实际伤害、原始伤害或两者。

**Architecture:** 在 `uiStore` 添加两个布尔状态控制伤害显示，`EditorToolbar` 增加 `DropdownMenu` 触发切换，`DamageEventCard` 根据状态动态渲染伤害文本和调整名称区域宽度。

**Tech Stack:** React, Zustand, Radix UI DropdownMenu (shadcn/ui), React-Konva, Lucide Icons

---

## 文件结构

| 操作   | 文件                                          | 职责                                                        |
| ------ | --------------------------------------------- | ----------------------------------------------------------- |
| Modify | `src/store/uiStore.ts`                        | 添加 `showActualDamage`、`showOriginalDamage` 状态和 toggle |
| Modify | `src/components/EditorToolbar.tsx`            | 添加视图下拉菜单按钮                                        |
| Modify | `src/components/Timeline/DamageEventCard.tsx` | 根据视图状态渲染伤害文本                                    |

---

### Task 1: uiStore 添加视图状态

**Files:**

- Modify: `src/store/uiStore.ts`

- [ ] **Step 1: 在 `UIState` 接口中添加状态和方法声明**

在 `isDamageTrackCollapsed: boolean` 之后添加：

```typescript
/** 是否显示实际伤害 */
showActualDamage: boolean
/** 是否显示原始伤害 */
showOriginalDamage: boolean
```

在 `toggleDamageTrackCollapsed: () => void` 之后添加：

```typescript
/** 切换显示实际伤害 */
toggleShowActualDamage: () => void
/** 切换显示原始伤害 */
toggleShowOriginalDamage: () => void
```

- [ ] **Step 2: 在 store 初始状态中添加默认值**

在 `isDamageTrackCollapsed: false,` 之后添加：

```typescript
showActualDamage: true,
showOriginalDamage: false,
```

- [ ] **Step 3: 在 store 中添加 toggle 方法**

在 `toggleDamageTrackCollapsed` 方法之后添加：

```typescript
toggleShowActualDamage: () =>
  set(state => ({
    showActualDamage: !state.showActualDamage,
  })),

toggleShowOriginalDamage: () =>
  set(state => ({
    showOriginalDamage: !state.showOriginalDamage,
  })),
```

- [ ] **Step 4: 验证编译通过**

Run: `pnpm exec tsc --noEmit`
Expected: 无报错

- [ ] **Step 5: Commit**

```bash
git add src/store/uiStore.ts
git commit -m "feat: uiStore 添加伤害显示视图状态"
```

---

### Task 2: EditorToolbar 添加视图下拉菜单

**Files:**

- Modify: `src/components/EditorToolbar.tsx`

- [ ] **Step 1: 添加 import**

在已有的 lucide-react import 中添加 `Eye`：

```typescript
import {
  ZoomIn,
  ZoomOut,
  Lock,
  Unlock,
  BugPlay,
  Undo2,
  Redo2,
  TriangleAlert,
  Settings,
  Eye,
} from 'lucide-react'
```

添加 DropdownMenu 组件 import：

```typescript
import {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuSub,
  DropdownMenuSubTrigger,
  DropdownMenuSubContent,
  DropdownMenuCheckboxItem,
} from '@/components/ui/dropdown-menu'
```

- [ ] **Step 2: 从 uiStore 解构新状态**

在 `const { toggleReadOnly } = useUIStore()` 行改为：

```typescript
const {
  toggleReadOnly,
  showActualDamage,
  showOriginalDamage,
  toggleShowActualDamage,
  toggleShowOriginalDamage,
} = useUIStore()
```

- [ ] **Step 3: 在锁定按钮和分隔线之间插入视图菜单**

在锁定/回放按钮的 `</Tooltip>` 结束标签（即 `{/* Replay Mode / Read-Only Toggle */}` 区块的结尾 `)}` 之后，`<div className="w-px h-6 bg-border mx-1" />` 分隔线之前，插入视图菜单：

```tsx
{
  /* 视图菜单 */
}
;<DropdownMenu>
  <Tooltip>
    <TooltipTrigger asChild>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon" className="h-7 w-7">
          <Eye className="w-4 h-4" />
        </Button>
      </DropdownMenuTrigger>
    </TooltipTrigger>
    <TooltipContent side="bottom">视图</TooltipContent>
  </Tooltip>
  <DropdownMenuContent align="start">
    <DropdownMenuSub>
      <DropdownMenuSubTrigger>伤害事件</DropdownMenuSubTrigger>
      <DropdownMenuSubContent>
        <DropdownMenuCheckboxItem
          checked={showActualDamage}
          onCheckedChange={toggleShowActualDamage}
        >
          实际伤害
        </DropdownMenuCheckboxItem>
        <DropdownMenuCheckboxItem
          checked={showOriginalDamage}
          onCheckedChange={toggleShowOriginalDamage}
        >
          原始伤害
        </DropdownMenuCheckboxItem>
      </DropdownMenuSubContent>
    </DropdownMenuSub>
  </DropdownMenuContent>
</DropdownMenu>
```

- [ ] **Step 4: 验证编译通过**

Run: `pnpm exec tsc --noEmit`
Expected: 无报错

- [ ] **Step 5: Commit**

```bash
git add src/components/EditorToolbar.tsx
git commit -m "feat: 工具栏添加视图下拉菜单"
```

---

### Task 3: DamageEventCard 支持视图切换

**Files:**

- Modify: `src/components/Timeline/DamageEventCard.tsx`

- [ ] **Step 1: 添加 uiStore import**

```typescript
import { useUIStore } from '@/store/uiStore'
```

- [ ] **Step 2: 在组件内读取视图状态**

在 `const colors = useCanvasColors()` 之后添加：

```typescript
const showActualDamage = useUIStore(s => s.showActualDamage)
const showOriginalDamage = useUIStore(s => s.showOriginalDamage)
```

- [ ] **Step 3: 提取数值格式化为独立函数**

在 `getDamageText` 之前添加格式化辅助函数：

```typescript
const formatDamageValue = (value: number): string => {
  return value >= 10000 ? `${(value / 10000).toFixed(1)}w` : value.toLocaleString()
}
```

- [ ] **Step 4: 重写 `getDamageText()` 函数**

替换现有的 `getDamageText` 函数为：

```typescript
const getDamageText = (): string => {
  if (isTankbuster) return '死刑'
  if (!showActualDamage && !showOriginalDamage) return ''

  const actualValue = hasOverkill ? calculatedEvent?.maxDamage : calculatedEvent?.finalDamage
  const originalValue = calculatedEvent?.originalDamage

  if (showActualDamage && showOriginalDamage) {
    if (actualValue === undefined || originalValue === undefined) return ''
    return `${formatDamageValue(actualValue)}/${formatDamageValue(originalValue)}`
  }
  if (showActualDamage) {
    if (actualValue === undefined) return ''
    return formatDamageValue(actualValue)
  }
  if (showOriginalDamage) {
    if (originalValue === undefined) return ''
    return formatDamageValue(originalValue)
  }
  return ''
}
```

- [ ] **Step 5: 调整名称区域和数值区域宽度**

替换现有的 `nameAreaWidth` 计算为：

```typescript
const showBoth = showActualDamage && showOriginalDamage
const damageTextWidth = !damageText ? 0 : showBoth ? 80 : 50
const nameAreaWidth = 150 - 5 - damageTextWidth
```

更新数值 `<Text>` 组件的 `x` 和 `width` 属性：

```tsx
{
  damageText && (
    <Text
      x={150 - damageTextWidth}
      y={-15}
      width={damageTextWidth - 5}
      height={30}
      text={damageText}
      fontSize={12}
      fill={colors.textPrimary}
      fontFamily="Arial, sans-serif"
      wrap="none"
      align="right"
      verticalAlign="middle"
      perfectDrawEnabled={false}
      listening={false}
    />
  )
}
```

- [ ] **Step 6: 更新 `nameXOffset` 和 `displayName` 中的宽度引用**

确保 `displayName` 的 `truncateText` 调用使用更新后的 `nameAreaWidth`（代码已引用 `nameAreaWidth` 变量，无需改变调用方式，只需确认变量已更新）。

- [ ] **Step 7: 验证编译通过**

Run: `pnpm exec tsc --noEmit`
Expected: 无报错

- [ ] **Step 8: 运行测试**

Run: `pnpm test:run`
Expected: 全部通过

- [ ] **Step 9: Commit**

```bash
git add src/components/Timeline/DamageEventCard.tsx
git commit -m "feat: 伤害事件卡片支持实际/原始伤害视图切换"
```
