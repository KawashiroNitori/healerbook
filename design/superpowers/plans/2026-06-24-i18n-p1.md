# i18n P1（基础设施）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Healerbook 搭建 react-i18next 国际化基础设施、Crowdin 全自动翻译流水线、后端语言传递接缝，并以 HomePage 头部为样板完成一处文案抽取。

**Architecture:** 纯运行时 react-i18next；语言状态仿现有 `theme` 范式持久化进 `uiStore`（不进 URL）；catalog 以 zh-CN 为唯一真源，其余 5 语由 Crowdin 回流；后端经 Hono 中间件读取前端注入的 `Accept-Language` 并向 FFLogs 客户端铺线（本期不改变输出）。

**Tech Stack:** i18next + react-i18next + i18next-icu + i18next-browser-languagedetector + i18next-parser + @crowdin/cli + crowdin/github-action；Vite 7 / React 19 / TS 5.9 / Hono / Zustand 5 / Vitest 4。

## Global Constraints

- 包管理器必须用 **pnpm**。
- TS 严格：`strict` + `verbatimModuleSyntax` + `erasableSyntaxOnly`；类型导入必须 `import type`，禁用 enum/namespace 及编译期宏。
- 提交信息/作者/Co-Authored-By **禁止**包含 "Claude"（`.husky/commit-msg` 拒绝，大小写不敏感）。
- 纯客户端 SPA，无 SSR。
- 后端 Worker 为 Hono（`src/workers/index.ts` + `routes/*.ts`）。
- 测试与源码同目录 `*.test.ts`；Vitest 默认 env 为 `node`，组件/需 DOM 的测试在文件顶部加 `// @vitest-environment jsdom`。
- 源语言 catalog `src/i18n/locales/zh-CN/*.json` 是唯一手写真源；其余语言目录为 Crowdin 产物，不手动编辑。
- 后端接缝**只铺线不分支**：FFLogs 的 `translate: false` 保持不变，输出与现状一致。

---

## 文件结构

| 路径                                                            | 创建/修改 | 职责                                                                                                                        |
| --------------------------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------- |
| `src/types/i18n.ts`                                             | 创建      | `AppLanguage`、`SUPPORTED_LOCALES`、`DEFAULT_LOCALE`、`LOCALE_LABELS`、`isAppLanguage`、`normalizeLocale`（纯，前后端共用） |
| `src/i18n/index.ts`                                             | 创建      | i18next 实例初始化、`getInitialLocale`、`getKonvaFontFamily`、`NAMESPACES`                                                  |
| `src/i18n/locales/zh-CN/{common,home,editor,import,share}.json` | 创建      | zh-CN 真源 catalog                                                                                                          |
| `src/store/uiStore.ts`                                          | 修改      | 新增 `locale` 状态 + `setLocale` + 模块级 `applyLocale`                                                                     |
| `src/components/LanguageToggle.tsx`                             | 创建      | 语言切换器                                                                                                                  |
| `src/pages/HomePage.tsx`                                        | 修改      | 挂载 `LanguageToggle` + 样板文案抽取                                                                                        |
| `src/pages/EditorPage.tsx`                                      | 修改      | 挂载 `LanguageToggle`                                                                                                       |
| `src/components/Timeline/SkillTracksCanvas.tsx`                 | 修改      | Konva "空转" 抽取 + locale 感知 `fontFamily`                                                                                |
| `src/api/apiClient.ts`                                          | 修改      | `beforeRequest` 注入 `Accept-Language`                                                                                      |
| `src/workers/middleware/readLanguage.ts`                        | 创建      | Hono 中间件，解析请求语言写入 `c`                                                                                           |
| `src/workers/env.ts`                                            | 修改      | `AppEnv.Variables` 增加 `lang`                                                                                              |
| `src/workers/routes/fflogs.ts`                                  | 修改      | 挂载中间件 + 把 `lang` 传给 client                                                                                          |
| `src/workers/fflogsClientV2.ts`                                 | 修改      | `GetReportParams.lang` + 扩展点注释                                                                                         |
| `tsconfig.app.json`                                             | 修改      | 增加 `resolveJsonModule`                                                                                                    |
| `i18next-parser.config.js`                                      | 创建      | 抽取配置                                                                                                                    |
| `crowdin.yml`                                                   | 创建      | Crowdin 文件映射                                                                                                            |
| `.github/workflows/crowdin.yml`                                 | 创建      | CI 自动同步                                                                                                                 |
| `package.json`                                                  | 修改      | 依赖 + `i18n:extract` 脚本                                                                                                  |

---

### Task 1: 共享 AppLanguage 类型与 normalizeLocale

**Files:**

- Create: `src/types/i18n.ts`
- Test: `src/types/i18n.test.ts`
- Modify: `package.json`（安装依赖）

**Interfaces:**

- Produces:
  - `type AppLanguage = 'zh-CN' | 'zh-TW' | 'ja' | 'en' | 'de' | 'fr'`
  - `const SUPPORTED_LOCALES: readonly AppLanguage[]`
  - `const DEFAULT_LOCALE: AppLanguage`（`'zh-CN'`）
  - `const LOCALE_LABELS: Record<AppLanguage, string>`
  - `function isAppLanguage(value: unknown): value is AppLanguage`
  - `function normalizeLocale(raw: string | null | undefined): AppLanguage`

- [ ] **Step 1: 安装依赖**

Run:

```bash
pnpm add i18next react-i18next i18next-icu i18next-browser-languagedetector
pnpm add -D i18next-parser @crowdin/cli
```

Expected: `package.json` dependencies/devDependencies 新增上述包，`pnpm-lock.yaml` 更新。

- [ ] **Step 2: 写失败测试**

Create `src/types/i18n.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { normalizeLocale, isAppLanguage, DEFAULT_LOCALE } from './i18n'

describe('normalizeLocale', () => {
  it('maps Traditional Chinese variants to zh-TW', () => {
    expect(normalizeLocale('zh-TW')).toBe('zh-TW')
    expect(normalizeLocale('zh-HK')).toBe('zh-TW')
    expect(normalizeLocale('zh-Hant')).toBe('zh-TW')
    expect(normalizeLocale('zh-Hant-TW')).toBe('zh-TW')
  })
  it('maps other Chinese to zh-CN', () => {
    expect(normalizeLocale('zh')).toBe('zh-CN')
    expect(normalizeLocale('zh-CN')).toBe('zh-CN')
    expect(normalizeLocale('zh-Hans')).toBe('zh-CN')
  })
  it('maps en/ja/de/fr by prefix', () => {
    expect(normalizeLocale('en-US')).toBe('en')
    expect(normalizeLocale('ja-JP')).toBe('ja')
    expect(normalizeLocale('de')).toBe('de')
    expect(normalizeLocale('fr-FR')).toBe('fr')
  })
  it('falls back to default for unknown/empty', () => {
    expect(normalizeLocale('ko')).toBe(DEFAULT_LOCALE)
    expect(normalizeLocale('')).toBe(DEFAULT_LOCALE)
    expect(normalizeLocale(null)).toBe(DEFAULT_LOCALE)
    expect(normalizeLocale(undefined)).toBe(DEFAULT_LOCALE)
  })
})

describe('isAppLanguage', () => {
  it('accepts supported, rejects others', () => {
    expect(isAppLanguage('en')).toBe(true)
    expect(isAppLanguage('zh-CN')).toBe(true)
    expect(isAppLanguage('ko')).toBe(false)
    expect(isAppLanguage(null)).toBe(false)
  })
})
```

- [ ] **Step 3: 运行测试确认失败**

Run: `pnpm test:run src/types/i18n.test.ts`
Expected: FAIL（`Cannot find module './i18n'`）

- [ ] **Step 4: 实现 `src/types/i18n.ts`**

```ts
/** 应用支持的界面语言。zh-CN 为源语言。前端与 Worker 共用。 */
export type AppLanguage = 'zh-CN' | 'zh-TW' | 'ja' | 'en' | 'de' | 'fr'

export const SUPPORTED_LOCALES: readonly AppLanguage[] = ['zh-CN', 'zh-TW', 'ja', 'en', 'de', 'fr']

export const DEFAULT_LOCALE: AppLanguage = 'zh-CN'

/** 语言切换器显示名（用各自语言的自称） */
export const LOCALE_LABELS: Record<AppLanguage, string> = {
  'zh-CN': '简体中文',
  'zh-TW': '繁體中文',
  ja: '日本語',
  en: 'English',
  de: 'Deutsch',
  fr: 'Français',
}

export function isAppLanguage(value: unknown): value is AppLanguage {
  return typeof value === 'string' && (SUPPORTED_LOCALES as readonly string[]).includes(value)
}

/**
 * 把任意 BCP-47 标签 / 存储值归一为受支持的 AppLanguage。
 * 未识别一律回退 DEFAULT_LOCALE。
 */
export function normalizeLocale(raw: string | null | undefined): AppLanguage {
  if (!raw) return DEFAULT_LOCALE
  const lower = raw.toLowerCase()
  if (lower.startsWith('zh')) {
    if (
      lower.includes('tw') ||
      lower.includes('hk') ||
      lower.includes('mo') ||
      lower.includes('hant')
    ) {
      return 'zh-TW'
    }
    return 'zh-CN'
  }
  if (lower.startsWith('ja')) return 'ja'
  if (lower.startsWith('en')) return 'en'
  if (lower.startsWith('de')) return 'de'
  if (lower.startsWith('fr')) return 'fr'
  return DEFAULT_LOCALE
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm test:run src/types/i18n.test.ts`
Expected: PASS（全部用例）

- [ ] **Step 6: Commit**

```bash
git add src/types/i18n.ts src/types/i18n.test.ts package.json pnpm-lock.yaml
git commit -m "feat(i18n): 新增共享 AppLanguage 类型与 locale 归一逻辑"
```

---

### Task 2: i18n 实例初始化与 zh-CN catalog

**Files:**

- Create: `src/i18n/index.ts`
- Create: `src/i18n/locales/zh-CN/common.json`、`home.json`、`editor.json`、`import.json`、`share.json`
- Test: `src/i18n/index.test.ts`
- Modify: `tsconfig.app.json`、`src/main.tsx`

**Interfaces:**

- Consumes: `AppLanguage`、`DEFAULT_LOCALE`、`SUPPORTED_LOCALES`、`normalizeLocale`、`isAppLanguage`（Task 1）
- Produces:
  - default export: 已初始化的 `i18n`（`i18next` 实例，含 `.t`/`.changeLanguage`）
  - `function getInitialLocale(): AppLanguage`
  - `function getKonvaFontFamily(locale: AppLanguage): string`
  - `const NAMESPACES: readonly string[]`

- [ ] **Step 1: 允许 JSON 导入**

Modify `tsconfig.app.json`，在 `compilerOptions` 内增加一行（与 `verbatimModuleSyntax` 同级）：

```jsonc
    "resolveJsonModule": true,
```

- [ ] **Step 2: 创建 zh-CN catalog 文件**

Create `src/i18n/locales/zh-CN/common.json`:

```json
{
  "operationFailed": "操作失败：{{message}}",
  "unknownError": "未知错误"
}
```

Create `src/i18n/locales/zh-CN/home.json`:

```json
{
  "subtitle": "FF14 减伤规划工具",
  "help": "帮助",
  "about": "关于"
}
```

Create `src/i18n/locales/zh-CN/editor.json`:

```json
{
  "idle": "空转 {{sec}}s"
}
```

Create `src/i18n/locales/zh-CN/import.json`:

```json
{}
```

Create `src/i18n/locales/zh-CN/share.json`:

```json
{}
```

- [ ] **Step 3: 写失败测试**

Create `src/i18n/index.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import i18n, { getKonvaFontFamily, NAMESPACES } from './index'

describe('i18n instance', () => {
  it('initializes with zh-CN and resolves a known key', () => {
    expect(i18n.t('home:help')).toBe('帮助')
  })
  it('interpolates ICU/i18next placeholders', () => {
    expect(i18n.t('common:operationFailed', { message: 'X' })).toBe('操作失败：X')
  })
  it('registers all five namespaces', () => {
    expect(NAMESPACES).toEqual(['common', 'home', 'editor', 'import', 'share'])
  })
})

describe('getKonvaFontFamily', () => {
  it('returns a CJK stack for zh and ja, latin for others', () => {
    expect(getKonvaFontFamily('zh-CN')).toContain('PingFang SC')
    expect(getKonvaFontFamily('zh-TW')).toContain('PingFang TC')
    expect(getKonvaFontFamily('ja')).toContain('Hiragino')
    expect(getKonvaFontFamily('en')).toBe('Arial, sans-serif')
  })
})
```

- [ ] **Step 4: 运行测试确认失败**

Run: `pnpm test:run src/i18n/index.test.ts`
Expected: FAIL（`Cannot find module './index'`）

- [ ] **Step 5: 实现 `src/i18n/index.ts`**

```ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import ICU from 'i18next-icu'
import {
  DEFAULT_LOCALE,
  SUPPORTED_LOCALES,
  isAppLanguage,
  normalizeLocale,
  type AppLanguage,
} from '@/types/i18n'

import common from './locales/zh-CN/common.json'
import home from './locales/zh-CN/home.json'
import editor from './locales/zh-CN/editor.json'
import importNs from './locales/zh-CN/import.json'
import share from './locales/zh-CN/share.json'

export const NAMESPACES = ['common', 'home', 'editor', 'import', 'share'] as const

/** localStorage('locale') → navigator.language → DEFAULT_LOCALE */
export function getInitialLocale(): AppLanguage {
  if (typeof localStorage !== 'undefined') {
    const stored = localStorage.getItem('locale')
    if (isAppLanguage(stored)) return stored
  }
  if (typeof navigator !== 'undefined' && navigator.language) {
    return normalizeLocale(navigator.language)
  }
  return DEFAULT_LOCALE
}

const CJK_SC =
  '"PingFang SC", "Microsoft YaHei", "Hiragino Sans GB", "Noto Sans CJK SC", sans-serif'
const CJK_TC = '"PingFang TC", "Microsoft JhengHei", "Noto Sans CJK TC", sans-serif'
const JP = '"Hiragino Kaku Gothic ProN", "Yu Gothic", Meiryo, "Noto Sans CJK JP", sans-serif'
const LATIN = 'Arial, sans-serif'

/** Konva <Text> 不走 DOM，需按 locale 显式选字体栈（系统字体，不自托管） */
export function getKonvaFontFamily(locale: AppLanguage): string {
  switch (locale) {
    case 'zh-CN':
      return CJK_SC
    case 'zh-TW':
      return CJK_TC
    case 'ja':
      return JP
    default:
      return LATIN
  }
}

void i18n
  .use(ICU)
  .use(initReactI18next)
  .init({
    lng: getInitialLocale(),
    fallbackLng: DEFAULT_LOCALE,
    supportedLngs: SUPPORTED_LOCALES as unknown as string[],
    ns: NAMESPACES as unknown as string[],
    defaultNS: 'common',
    returnNull: false,
    interpolation: { escapeValue: false },
    resources: {
      'zh-CN': { common, home, editor, import: importNs, share },
    },
  })

export default i18n
```

- [ ] **Step 6: 在入口初始化 i18n**

Modify `src/main.tsx`，在 `import './index.css'` 之后增加副作用导入：

```ts
import './index.css'
import './i18n'
```

- [ ] **Step 7: 运行测试 + 类型检查**

Run: `pnpm test:run src/i18n/index.test.ts && pnpm exec tsc --noEmit`
Expected: 测试 PASS；tsc 无错误。

- [ ] **Step 8: Commit**

```bash
git add src/i18n tsconfig.app.json src/main.tsx
git commit -m "feat(i18n): 初始化 i18next 实例与 zh-CN catalog"
```

---

### Task 3: uiStore 增加 locale 状态

**Files:**

- Modify: `src/store/uiStore.ts`
- Test: `src/store/uiStore.locale.test.ts`

**Interfaces:**

- Consumes: default `i18n`、`getInitialLocale`（Task 2）；`AppLanguage`（Task 1）
- Produces: `useUIStore` 新增 `locale: AppLanguage` 与 `setLocale: (locale: AppLanguage) => void`

- [ ] **Step 1: 写失败测试**

Create `src/store/uiStore.locale.test.ts`:

```ts
// @vitest-environment jsdom
import { describe, it, expect, vi, beforeEach } from 'vitest'

const changeLanguage = vi.fn()
vi.mock('@/i18n', () => ({
  default: { changeLanguage },
  getInitialLocale: () => 'zh-CN',
  getKonvaFontFamily: () => 'Arial, sans-serif',
  NAMESPACES: ['common'],
}))

import { useUIStore } from './uiStore'

describe('uiStore locale', () => {
  beforeEach(() => {
    changeLanguage.mockClear()
    localStorage.clear()
  })
  it('defaults to getInitialLocale()', () => {
    expect(useUIStore.getState().locale).toBe('zh-CN')
  })
  it('setLocale writes localStorage and calls i18n.changeLanguage', () => {
    useUIStore.getState().setLocale('en')
    expect(useUIStore.getState().locale).toBe('en')
    expect(localStorage.getItem('locale')).toBe('en')
    expect(changeLanguage).toHaveBeenCalledWith('en')
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:run src/store/uiStore.locale.test.ts`
Expected: FAIL（`locale` 未定义 / `setLocale is not a function`）

- [ ] **Step 3: 修改 `src/store/uiStore.ts`**

在文件顶部 import 区（`import { persist } ...` 之后）增加：

```ts
import i18n, { getInitialLocale } from '@/i18n'
import type { AppLanguage } from '@/types/i18n'
```

在 `interface UIState` 内，`theme` 字段后增加：

```ts
/** 当前界面语言 */
locale: AppLanguage
```

在 actions 区 `setTheme` 声明后增加：

```ts
  /** 设置界面语言 */
  setLocale: (locale: AppLanguage) => void
```

在模块级 `applyTheme`/`getInitialTheme` 之后增加：

```ts
function applyLocale(locale: AppLanguage) {
  if (typeof localStorage !== 'undefined') {
    localStorage.setItem('locale', locale)
  }
  void i18n.changeLanguage(locale)
}

const initialLocale = getInitialLocale()
```

在 `create` 的初始 state 中，`theme: initialTheme,` 后增加：

```ts
      locale: initialLocale,
```

在 `setTheme` 实现后增加：

```ts
      setLocale: locale => {
        applyLocale(locale)
        set({ locale })
      },
```

把 persist 的 `partialize` 改为同时排除 `locale`（与 `theme` 一致，用独立 key）：

```ts
      // eslint-disable-next-line @typescript-eslint/no-unused-vars
      partialize: ({ theme, locale, draggingId, manualLock, ...rest }) => rest,
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:run src/store/uiStore.locale.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/store/uiStore.ts src/store/uiStore.locale.test.ts
git commit -m "feat(i18n): uiStore 新增 locale 状态（仿 theme 持久化）"
```

---

### Task 4: LanguageToggle 组件与挂载

**Files:**

- Create: `src/components/LanguageToggle.tsx`
- Test: `src/components/LanguageToggle.test.tsx`
- Modify: `src/pages/HomePage.tsx`、`src/pages/EditorPage.tsx`

**Interfaces:**

- Consumes: `useUIStore`（`locale`/`setLocale`，Task 3）；`SUPPORTED_LOCALES`、`LOCALE_LABELS`、`AppLanguage`（Task 1）
- Produces: default export `LanguageToggle`（React 组件）

- [ ] **Step 1: 写失败测试**

Create `src/components/LanguageToggle.test.tsx`:

```tsx
// @vitest-environment jsdom
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'

const setLocale = vi.fn()
vi.mock('@/store/uiStore', () => ({
  useUIStore: (sel: (s: { locale: string; setLocale: typeof setLocale }) => unknown) =>
    sel({ locale: 'zh-CN', setLocale }),
}))

import LanguageToggle from './LanguageToggle'

describe('LanguageToggle', () => {
  it('opens menu and switching a language calls setLocale', () => {
    render(<LanguageToggle />)
    fireEvent.click(screen.getByRole('button', { name: 'Language' }))
    fireEvent.click(screen.getByText('English'))
    expect(setLocale).toHaveBeenCalledWith('en')
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:run src/components/LanguageToggle.test.tsx`
Expected: FAIL（`Cannot find module './LanguageToggle'`）

- [ ] **Step 3: 实现 `src/components/LanguageToggle.tsx`**

```tsx
import { Languages } from 'lucide-react'
import { useUIStore } from '@/store/uiStore'
import { SUPPORTED_LOCALES, LOCALE_LABELS, type AppLanguage } from '@/types/i18n'
import { Button } from '@/components/ui/button'
import {
  DropdownMenu,
  DropdownMenuTrigger,
  DropdownMenuContent,
  DropdownMenuRadioGroup,
  DropdownMenuRadioItem,
} from '@/components/ui/dropdown-menu'

export default function LanguageToggle() {
  const locale = useUIStore(s => s.locale)
  const setLocale = useUIStore(s => s.setLocale)

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button variant="ghost" size="icon" aria-label="Language">
          <Languages className="w-5 h-5" />
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuRadioGroup value={locale} onValueChange={v => setLocale(v as AppLanguage)}>
          {SUPPORTED_LOCALES.map(l => (
            <DropdownMenuRadioItem key={l} value={l}>
              {LOCALE_LABELS[l]}
            </DropdownMenuRadioItem>
          ))}
        </DropdownMenuRadioGroup>
      </DropdownMenuContent>
    </DropdownMenu>
  )
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:run src/components/LanguageToggle.test.tsx`
Expected: PASS

- [ ] **Step 5: 挂载到 HomePage**

Modify `src/pages/HomePage.tsx`：在文件已有 import 区增加 `import LanguageToggle from '@/components/LanguageToggle'`。把 header 右侧（约 `:163`）的 `<ThemeToggle />` 改为：

```tsx
            <ThemeToggle />
            <LanguageToggle />
            <AuthButton />
```

- [ ] **Step 6: 挂载到 EditorPage**

Modify `src/pages/EditorPage.tsx`：增加 `import LanguageToggle from '@/components/LanguageToggle'`。把 header 右侧（约 `:385`）的 `<ThemeToggle />` 改为：

```tsx
            <PresenceAvatars />
            <ThemeToggle />
            <LanguageToggle />
```

- [ ] **Step 7: 类型检查 + lint**

Run: `pnpm exec tsc --noEmit && pnpm lint`
Expected: 无错误。

- [ ] **Step 8: Commit**

```bash
git add src/components/LanguageToggle.tsx src/components/LanguageToggle.test.tsx src/pages/HomePage.tsx src/pages/EditorPage.tsx
git commit -m "feat(i18n): 新增语言切换器并挂载到两个页面 header"
```

---

### Task 5: 样板文案抽取（HomePage 头部）

**Files:**

- Modify: `src/pages/HomePage.tsx`

**Interfaces:**

- Consumes: `useTranslation`（react-i18next）；catalog 键 `home:subtitle`/`home:help`/`home:about`、`common:operationFailed`/`common:unknownError`（Task 2）

- [ ] **Step 1: 引入 useTranslation**

Modify `src/pages/HomePage.tsx`：增加 `import { useTranslation } from 'react-i18next'`。在组件函数体顶部（其他 hook 旁）增加：

```tsx
const { t } = useTranslation(['home', 'common'])
```

- [ ] **Step 2: 替换副标题**

把 `:114` 的：

```tsx
<p className="hidden sm:block text-sm text-muted-foreground">FF14 减伤规划工具</p>
```

改为：

```tsx
<p className="hidden sm:block text-sm text-muted-foreground">{t('home:subtitle')}</p>
```

- [ ] **Step 3: 替换"帮助""关于"**

把 `:126` 的 `<span className="hidden sm:inline">帮助</span>` 改为：

```tsx
<span className="hidden sm:inline">{t('home:help')}</span>
```

把 `:140` 的 `<span className="hidden sm:inline">关于</span>` 改为：

```tsx
<span className="hidden sm:inline">{t('home:about')}</span>
```

- [ ] **Step 4: 替换 toast 模板**

把 `:100` 的：

```tsx
toast.error(`操作失败：${err instanceof Error ? err.message : '未知错误'}`)
```

改为：

```tsx
toast.error(
  t('common:operationFailed', {
    message: err instanceof Error ? err.message : t('common:unknownError'),
  })
)
```

- [ ] **Step 5: 验证渲染、类型、lint、构建**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm build`
Expected: 全部通过；构建产物正常。

- [ ] **Step 6: Commit**

```bash
git add src/pages/HomePage.tsx
git commit -m "feat(i18n): 抽取 HomePage 头部文案作为批量抽取范式"
```

---

### Task 6: Konva 画布文案与字体 locale 化

**Files:**

- Modify: `src/components/Timeline/SkillTracksCanvas.tsx`

**Interfaces:**

- Consumes: default `i18n`、`getKonvaFontFamily`（Task 2）；`useUIStore`（`locale`，Task 3）；catalog 键 `editor:idle`（Task 2）

- [ ] **Step 1: 引入 i18n、字体函数、locale 订阅**

Modify `src/components/Timeline/SkillTracksCanvas.tsx`：增加 import：

```ts
import i18n, { getKonvaFontFamily } from '@/i18n'
import { useUIStore } from '@/store/uiStore'
```

在组件函数体顶部（其他 hook 旁）增加（订阅 locale 以便切换语言时重绘）：

```ts
const locale = useUIStore(s => s.locale)
const konvaFont = getKonvaFontFamily(locale)
```

- [ ] **Step 2: 替换第一处"空转"文本（约 :482）**

把：

```tsx
                    text={`空转 ${firstIdleTime.toFixed(1)}s`}
                    fontSize={11}
                    fill="#f59e0b"
                    fontStyle="bold"
                    fontFamily="Arial, sans-serif"
```

改为：

```tsx
                    text={i18n.t('editor:idle', { sec: firstIdleTime.toFixed(1) })}
                    fontSize={11}
                    fill="#f59e0b"
                    fontStyle="bold"
                    fontFamily={konvaFont}
```

- [ ] **Step 3: 替换第二处"空转"文本（约 :545）**

把：

```tsx
                  text={`空转 ${idleTime.toFixed(1)}s`}
                  fontSize={11}
                  fill="#f59e0b"
                  fontStyle="bold"
                  fontFamily="Arial, sans-serif"
```

改为：

```tsx
                  text={i18n.t('editor:idle', { sec: idleTime.toFixed(1) })}
                  fontSize={11}
                  fill="#f59e0b"
                  fontStyle="bold"
                  fontFamily={konvaFont}
```

- [ ] **Step 4: 验证类型、lint、构建**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm build`
Expected: 全部通过。

> 说明：`editor:idle` 含 `{{sec}}` 插值，`i18n.t(..., { sec })` 返回纯字符串供 Konva `<Text>` 使用；订阅 `locale` 保证语言切换时组件重渲染、文本与字体随之更新。

- [ ] **Step 5: Commit**

```bash
git add src/components/Timeline/SkillTracksCanvas.tsx
git commit -m "feat(i18n): Canvas 空转文案 i18n 化并按 locale 选字体"
```

---

### Task 7: apiClient 注入 Accept-Language

**Files:**

- Modify: `src/api/apiClient.ts`
- Test: `src/api/apiClient.test.ts`

**Interfaces:**

- Consumes: `useUIStore`（`locale`，Task 3）
- Produces: 导出 `attachAcceptLanguage`（`BeforeRequestHook`），并在 `apiClient` 的 `beforeRequest` 中启用

- [ ] **Step 1: 写失败测试**

Create `src/api/apiClient.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest'

vi.mock('@/store/uiStore', () => ({
  useUIStore: { getState: () => ({ locale: 'en' }) },
}))
vi.mock('@/store/authStore', () => ({
  useAuthStore: { getState: () => ({ getValidToken: async () => null }) },
}))

import { attachAcceptLanguage } from './apiClient'

describe('attachAcceptLanguage', () => {
  it('sets Accept-Language to the current store locale', () => {
    const request = new Request('https://example.com')
    attachAcceptLanguage(request, {} as never)
    expect(request.headers.get('Accept-Language')).toBe('en')
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:run src/api/apiClient.test.ts`
Expected: FAIL（`attachAcceptLanguage` 未导出）

- [ ] **Step 3: 修改 `src/api/apiClient.ts`**

把 import 行：

```ts
import ky, { type AfterResponseHook, type BeforeErrorHook, HTTPError } from 'ky'
import { useAuthStore } from '@/store/authStore'
```

改为：

```ts
import ky, {
  type AfterResponseHook,
  type BeforeErrorHook,
  type BeforeRequestHook,
  HTTPError,
} from 'ky'
import { useAuthStore } from '@/store/authStore'
import { useUIStore } from '@/store/uiStore'
```

在 `attachParsedError` 定义之后增加：

```ts
/** 用站内选定 locale 覆盖浏览器默认 Accept-Language，供后端按用户真实选择取数 */
export const attachAcceptLanguage: BeforeRequestHook = request => {
  request.headers.set('Accept-Language', useUIStore.getState().locale)
}
```

把 `beforeRequest` 数组改为追加该 hook：

```ts
    beforeRequest: [
      async request => {
        const token = await useAuthStore.getState().getValidToken()
        if (token) {
          request.headers.set('Authorization', `Bearer ${token}`)
        }
      },
      attachAcceptLanguage,
    ],
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test:run src/api/apiClient.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/api/apiClient.ts src/api/apiClient.test.ts
git commit -m "feat(i18n): apiClient 注入站内 locale 至 Accept-Language"
```

---

### Task 8: Worker 语言中间件与 FFLogs 接缝

**Files:**

- Create: `src/workers/middleware/readLanguage.ts`
- Test: `src/workers/middleware/readLanguage.test.ts`
- Modify: `src/workers/env.ts`、`src/workers/routes/fflogs.ts`、`src/workers/fflogsClientV2.ts`

**Interfaces:**

- Consumes: `normalizeLocale`、`AppLanguage`（Task 1）
- Produces: `const readLanguage: MiddlewareHandler<AppEnv>`；`AppEnv.Variables.lang?: AppLanguage`

- [ ] **Step 1: 写失败测试**

Create `src/workers/middleware/readLanguage.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { Hono } from 'hono'
import type { AppEnv } from '../env'
import { readLanguage } from './readLanguage'

function makeApp() {
  const app = new Hono<AppEnv>()
  app.use('*', readLanguage)
  app.get('/x', c => c.json({ lang: c.get('lang') }))
  return app
}

describe('readLanguage middleware', () => {
  it('parses Accept-Language into AppLanguage', async () => {
    const res = await makeApp().request('/x', { headers: { 'Accept-Language': 'en-US' } })
    expect(await res.json()).toEqual({ lang: 'en' })
  })
  it('falls back to default when header missing', async () => {
    const res = await makeApp().request('/x')
    expect(await res.json()).toEqual({ lang: 'zh-CN' })
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test:run src/workers/middleware/readLanguage.test.ts`
Expected: FAIL（`Cannot find module './readLanguage'`）

- [ ] **Step 3: 在 env.ts 增加 Variables.lang**

Modify `src/workers/env.ts`：顶部增加 `import type { AppLanguage } from '@/types/i18n'`。把：

```ts
export type AppEnv = {
  Bindings: Env
  Variables: {
    auth?: { userId: string; username: string }
  }
}
```

改为：

```ts
export type AppEnv = {
  Bindings: Env
  Variables: {
    auth?: { userId: string; username: string }
    /** 由 readLanguage 中间件解析的请求语言 */
    lang?: AppLanguage
  }
}
```

- [ ] **Step 4: 实现 `src/workers/middleware/readLanguage.ts`**

```ts
import type { MiddlewareHandler } from 'hono'
import type { AppEnv } from '../env'
import { normalizeLocale } from '@/types/i18n'

/**
 * 把请求的 Accept-Language 归一为 AppLanguage 写入 c.var.lang。
 * 前端 apiClient 会用站内 locale 覆盖该头；缺失时回退 DEFAULT_LOCALE。
 */
export const readLanguage: MiddlewareHandler<AppEnv> = async (c, next) => {
  c.set('lang', normalizeLocale(c.req.header('Accept-Language')))
  await next()
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm test:run src/workers/middleware/readLanguage.test.ts`
Expected: PASS

- [ ] **Step 6: 在 fflogs 路由挂载中间件并向下传 lang**

Modify `src/workers/routes/fflogs.ts`：在 `const app = new Hono<AppEnv>()` 之后增加：

```ts
import { readLanguage } from '../middleware/readLanguage'

app.use('*', readLanguage)
```

（`import` 放到文件顶部 import 区；`app.use` 放在路由声明之前。）

把 `/import` handler 里 `getEvents` 调用（约 :79-83）：

```ts
const eventsData = await client.getEvents({
  reportCode,
  start: fight.startTime,
  end: fight.endTime,
})
```

改为：

```ts
const eventsData = await client.getEvents({
  reportCode,
  start: fight.startTime,
  end: fight.endTime,
  lang: c.get('lang'),
})
```

把 `/import` handler 里 `getReport` 调用（约 :59）：

```ts
const report = await client.getReport({ reportCode })
```

改为：

```ts
const report = await client.getReport({ reportCode, lang: c.get('lang') })
```

把 `/report/:reportCode` handler（约 :18）：

```ts
const data = await client.getReport({ reportCode })
```

改为：

```ts
const data = await client.getReport({ reportCode, lang: c.get('lang') })
```

把 `/events/:reportCode` handler 的 `lang`（约 :29）来源从 query 改为上下文：

```ts
const lang = c.get('lang')
```

（删除原 `const lang = c.req.query('lang') || undefined` 行；`getEvents({ ..., lang })` 调用保持不变。）

- [ ] **Step 7: 在 client 增加 lang 形参与扩展点注释**

Modify `src/workers/fflogsClientV2.ts`：顶部增加 `import type { AppLanguage } from '@/types/i18n'`。

把 `GetReportParams` 类型（约 :24-26）扩展为带可选 `lang`：

```ts
interface GetReportParams {
  reportCode: string
  lang?: AppLanguage
}
```

把 `GetEventsParams` 中已有的 `lang?: string`（约 :35）改为 `lang?: AppLanguage`（统一类型）。

在 `getReport` 内 `masterData(translate: false)`（约 :380）上方增加注释：

```ts
// i18n 扩展点：未来可依据 params.lang 切换 translate / 数据源语言；
// 当前阶段保持 translate: false 不改变输出。
```

在 `getEvents` 内 events 查询 `translate: false`（约 :539）上方增加注释：

```ts
// i18n 扩展点：未来可依据 lang 切换 translate；当前保持 false。
```

- [ ] **Step 8: 验证类型、lint、Worker 测试**

Run: `pnpm exec tsc --noEmit && pnpm lint && pnpm test:run src/workers/middleware/readLanguage.test.ts`
Expected: 全部通过。

> CORS 说明：`Accept-Language` 是 CORS-safelisted 请求头，无需修改 `src/workers/index.ts:34` 的 `allowHeaders`。

- [ ] **Step 9: Commit**

```bash
git add src/workers/middleware/readLanguage.ts src/workers/middleware/readLanguage.test.ts src/workers/env.ts src/workers/routes/fflogs.ts src/workers/fflogsClientV2.ts
git commit -m "feat(i18n): Worker 解析请求语言并向 FFLogs 客户端铺线（不改输出）"
```

---

### Task 9: i18next-parser 抽取配置

**Files:**

- Create: `i18next-parser.config.js`
- Modify: `package.json`

**Interfaces:**

- Consumes: 命名空间约定（Task 2）
- Produces: `pnpm i18n:extract` 命令，扫描源码更新 `src/i18n/locales/zh-CN/*.json`

- [ ] **Step 1: 创建 `i18next-parser.config.js`**

```js
/** @type {import('i18next-parser').UserConfig} */
export default {
  locales: ['zh-CN'],
  defaultNamespace: 'common',
  namespaceSeparator: ':',
  keySeparator: '.',
  input: ['src/**/*.{ts,tsx}'],
  output: 'src/i18n/locales/$LOCALE/$NAMESPACE.json',
  sort: true,
  // 源语言文案由人手写进 catalog；parser 仅负责新增 key，新 key 默认空串
  defaultValue: '',
  // P1 用 true：避免经 i18n.t(...) 引用（lexer 可能识别不到）的 key 被误删
  keepRemoved: true,
}
```

- [ ] **Step 2: 增加 npm 脚本**

Modify `package.json` 的 `scripts`，增加：

```json
    "i18n:extract": "i18next",
```

- [ ] **Step 3: 运行抽取并核对**

Run: `pnpm i18n:extract`
Expected: 命令成功；`src/i18n/locales/zh-CN/home.json` 含 `help`/`about`/`subtitle` 键，`common.json` 含 `operationFailed`/`unknownError`，`editor.json` 含 `idle`。新增空 key 用 `git diff` 复核（样板已手填中文，不应被清空——若 parser 把已填值改空，确认 `defaultValue: ''` 只作用于新 key；如有被清空的现存值，用 `git checkout` 恢复对应文件后再排查）。

> 说明：parser 的职责是同步 key 结构与发现缺失/孤儿 key，不负责翻译；zh-CN 的中文值由开发者手写维护。

- [ ] **Step 4: Commit**

```bash
git add i18next-parser.config.js package.json src/i18n/locales/zh-CN
git commit -m "chore(i18n): 接入 i18next-parser 抽取配置与脚本"
```

---

### Task 10: Crowdin 配置与 CI 自动同步

**Files:**

- Create: `crowdin.yml`
- Create: `.github/workflows/crowdin.yml`

**Interfaces:**

- Consumes: `src/i18n/locales/zh-CN/*.json`（源串，Task 2/9）
- Produces: Crowdin 文件映射 + GitHub Action（push 上传源串、回流译文 PR）

- [ ] **Step 1: 创建 `crowdin.yml`**

```yaml
'project_id_env': 'CROWDIN_PROJECT_ID'
'api_token_env': 'CROWDIN_PERSONAL_TOKEN'
'base_path': '.'
'preserve_hierarchy': true

files:
  - source: '/src/i18n/locales/zh-CN/*.json'
    translation: '/src/i18n/locales/%locale%/%original_file_name%'
    languages_mapping:
      locale:
        zh-TW: 'zh-TW'
        ja: 'ja'
        en: 'en'
        de: 'de'
        fr: 'fr'
```

- [ ] **Step 2: 创建 `.github/workflows/crowdin.yml`**

```yaml
name: Crowdin Sync

on:
  push:
    branches: [main, feat/i18n]
    paths:
      - 'src/i18n/locales/zh-CN/**'
  workflow_dispatch:

jobs:
  synchronize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Crowdin push & pull
        uses: crowdin/github-action@v2
        with:
          upload_sources: true
          upload_translations: false
          download_translations: true
          localization_branch_name: l10n/crowdin
          create_pull_request: true
          pull_request_title: 'chore(i18n): 回流 Crowdin 译文'
          pull_request_base_branch_name: 'main'
        env:
          CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
          CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}
```

- [ ] **Step 3: 校验 YAML 与 crowdin 配置**

Run: `pnpm exec crowdin status --identity-env 2>/dev/null || echo "需配置 Secrets 后在 CI 生效"`
Expected: 本地无 token 时报缺凭证属正常（CI 用 Secrets）。`crowdin.yml`/workflow YAML 无语法错误（可用 `node -e "require('js-yaml')" ` 或交由 CI 校验；若本地无 js-yaml，肉眼核对缩进）。

> 前置（人工，CI 生效前）：在 Crowdin 建项目（源语言 Chinese Simplified、勾选 zh-TW/ja/en/de/fr）；在 GitHub 仓库 Secrets 配置 `CROWDIN_PROJECT_ID`、`CROWDIN_PERSONAL_TOKEN`。

- [ ] **Step 4: Commit**

```bash
git add crowdin.yml .github/workflows/crowdin.yml
git commit -m "chore(i18n): 接入 Crowdin 全自动翻译流水线"
```

---

## 收尾验证（全部任务完成后）

- [ ] Run: `pnpm test:run`（全量测试）— Expected: 全绿
- [ ] Run: `pnpm lint` — Expected: 无错误
- [ ] Run: `pnpm exec tsc --noEmit` — Expected: 无错误
- [ ] Run: `pnpm build` — Expected: 构建成功
- [ ] 手动：本地 `pnpm dev`，用 header 语言切换器切到 English/繁體，确认切换即时生效、刷新后保持（localStorage `locale`）、Canvas "空转" 文本与字体随语言变化。

---

## 自审记录

- **Spec 覆盖**：§3 选型→Task1/2；§4 模块→各 Task；§5 状态/归一→Task1/3；§6 命名空间→Task2；§7 Crowdin→Task9/10；§8 后端接缝→Task7/8；§9 Konva→Task6；§10 字体→Task2/6；§11 测试→各 Task 测试步骤 + 收尾。**actions/status 为 P2 预留空目录**：本期不创建文件，命名空间列表仅含 5 个（符合 spec「预留」语义，不构成缺口）。
- **占位符**：无 TBD/TODO；所有代码步骤含完整代码。
- **类型一致**：`AppLanguage`/`SUPPORTED_LOCALES`/`normalizeLocale`/`getInitialLocale`/`getKonvaFontFamily`/`setLocale`/`attachAcceptLanguage`/`readLanguage` 在定义与消费处签名一致。
